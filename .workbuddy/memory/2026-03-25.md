# 2026-03-25 Daily Memory

## TourGuide 项目 (领队工具)

### 服务器信息
- IP: 1.12.249.189
- SSH key: ~/.ssh/opencl.pem
- 项目路径: /www/领队工具/tourguide (后端) 和 /www/领队工具/tourguide-web (前端)
- PM2进程: tourguide (port 3000), tourguide-web (port 3001)

### 今日完成的功能

#### 发票管理模块 (Invoices)
1. **OCR识别**: 使用阿里云百炼 qwen-vl-ocr 模型识别发票
   - API Key: sk-f2cf019db0cc4a139391752a43209ef6 (DashScope)
   - 识别字段: title, amount, invoiceNo, invoiceDate, category, description
   - OCR prompt 已优化，强调金额必须有效数字、日期必须是YYYY-MM-DD格式

2. **去重规则**:
   - 上传时: 同一用户 + 相同文件名 → 拒绝
   - OCR识别后: 同一用户 + 相同发票号码 → 拒绝
   - 前端正确处理 res.data.data.success 嵌套结构

3. **字段合并**: title 和 fileName 合并，只保留 fileName

4. **前端展示优化**:
   - 全选按钮: 根据当前筛选状态选择
   - 发票卡片展示: fileName, amount, invoiceNo, invoiceDate, category, description
   - 修复了 null 字符串比较问题 (category !== 'null')

5. **PDF预览**: 修复了静态文件路径 /www/tourguide → /www/领队工具/tourguide

6. **环境变量**: ecosystem.config.js 配置了 DASHSCOPE_API_KEY 和 JWT_SECRET，pm2 save 已保存

### 待办/问题
- 发票金额(amount)识别效果不稳定，有时返回0
- 发票日期(invoiceDate)有时返回无效值 "0NaN-aN-aN"
- 已有发票需要重新上传才能更新 fileName（已改为保留原始文件名）

### 正式/测试环境配置
- **当前**: 测试环境，IP访问 (http://1.12.249.189)
- **正式环境**: 域名 (moladog.top)
- **重要**: PDF预览URL使用 `window.location.origin` 动态获取，当前端在不同端口或域名时自动适配
- **迁移到正式环境时**: 无需修改代码，URL会自动适配

### API配置
- JWT_SECRET: TourGuideSecretKey2024!@#$%^&*
- 静态文件: /www/领队工具/tourguide/uploads
