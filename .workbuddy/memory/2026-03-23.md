# 2026-03-23 工作记录

## 发票模块修复（进行中）

### 问题描述
- 上传发票文件名中文乱码（三重Latin-1编码问题）
- PDF预览显示乱码
- 文件上传后fileUrl为undefined（Node.js v14不支持randomUUID()）

### 修复方案（已部署待验证）
1. **循环解码**：在`findAll/findOne/optimize`返回前，用私有方法`fixStr()`循环解码（最多5次`Buffer.from(s,'latin1').toString('utf8')`）
2. **文件名生成**：改用`Math.random()`代替`randomUUID()`
3. **title传递**：通过URL query参数传递（避免multipart编码破坏）
4. **版本升级**：1.0.0 → 1.0.1 → 1.0.2 触发前端清缓存

### 文件路径
- 源码：`/www/tourguide/src/modules/invoices/invoices.service.ts`
- 模块配置：`/www/tourguide/src/modules/invoices/invoices.module.ts`
- 前端API：`/www/tourguide-web/src/services/api.js`
- 前端视图：`/www/tourguide-web/src/views/Invoices.vue`

### 待验证
- 子会话正在SSH连接验证，验证命令：
```python
import urllib.request,json
d=json.dumps({'username':'admin','password':'tourguide123'}).encode()
r=urllib.request.Request('http://localhost:3000/auth/login',data=d,headers={'Content-Type':'application/json'})
resp=json.loads(urllib.request.urlopen(r).read())
token=resp['data']['accessToken']
r2=urllib.request.Request('http://localhost:3000/invoices',headers={'Authorization':'Bearer '+token})
resp2=json.loads(urllib.request.urlopen(r2).read())
for inv in resp2.get('data',[]): print(inv.get('title','')[:40])
```

## 其他修复
- MulterModule添加（修复file.originalname undefined）
- 批量删除改用原子API
- 重置发票状态功能（POST /invoices/:id/reset）
- PDF预览弹窗
- 删除发票同步删除磁盘文件
- 前端双层缓存架构（Memory+localStorage）
- 后端版本更新触发前端清缓存

## 管理账号
- admin / tourguide123

## 服务器信息（当前）
- 腾讯云：1.12.249.189
- tourguide (后端): online, port 3000, uptime 4h+, restarts 469
- tourguide-web: online, port 3001 (combined-server代理3000)
- **tourguide-api: 进程缺失**（从PM2中消失，端口3002无监听）

## 外部访问状态
- HTTPS://moladog.top: ❌ SSL握手失败（外部无法访问）
- HTTP://moladog.top:3001: ✅ 正常返回200
- localhost:3000: ✅ 正常

## Nginx配置
- 443端口：nginx监听，本地测试正常，外部SSL握手失败（防火墙/安全组问题）
- 3001端口：combined-server，代理API到3000

## 管理账号
- admin / tourguide123

## 开发铁律（苗苗强调，永久生效）
1. 改代码前先读懂现有代码，不破坏现有功能
2. 业务逻辑全放后端，前端只做展示
3. 前端轻量本地缓存：GET自动缓存，更新后清缓存，版本更新清缓存
4. 文件上传用apiUpload实例（不用全局json header）
